
- ensure only supported: why? lol Just say it on the README
    -> if don't, it will just revert somewhere along the way.

- making decision to not calculate needed collateral on-chain. This calculation can be done off-chain.

- aave supply approve can be avoided with SupplyWithPermit?


UNIT TESTS:

Swap service:
- test that you get output tokens (fork)

Debt service:
- test that you get debt (fork)

Full short:

- test that all tokens are where they are supposed to be
- should test for every combo?
- we run into the problem of potential time variance


Husky Notes:
- npx husky install
- npx husky add .husky/commit-msg 'npx --no -- commitlint --edit "$1"'
- npx husky add .husky/pre-commit "npm run format && npm run lint:check && npm run test"

Uniswap Notes:
- cast call 0x1F98431c8aD98523631AE4a59f267346ea31F984 "getPool(address,address,uint24)" 0xDA10009cBd5D07dd0CeCc66161FC93D7c9000da1 0xaf88d065e77c8cC2239327C5EDb3A432268e5831 1000 --rpc-url https://arbitrum.llamarpc.com